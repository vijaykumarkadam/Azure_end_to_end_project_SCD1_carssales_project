# This is a Databricks asset bundle definition for car_sales_asset_bundle.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: car_sales_asset_bundle
  uuid: af6e9605-217f-4a02-b2b6-f75e9196a8e0

include:
  - resources/*.yml
  - resources/**/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    presets:
      name_prefix: dev_${workspace.current_user.short_name}
      source_linked_deployment : false
    workspace:
      host: http://adb-7405610163216298.18.azuredatabricks.net
  
  prod:
    mode: production
    workspace:
      host: http://adb-7405610163216298.18.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/vnathumahale_gmail.com#ext#@vnathumahalegmail.onmicrosoft.com to make sure we only have a single copy.
      root_path: /Workspace/cars_sales_project/Azure_end_to_end_project_SCD1_carssales_project/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - group_name: admins
        level: CAN_MANAGE
